<div class="col-md-3 border-menu">
    <ul class="nav nav-pills nav-stacked">
        <li><a href="@Url.Action("Profile","Home")">My profile</a></li>
        <li><a href="@Url.Action("Index","Messages")">Messages @if (ViewBag.UnRead != 0)
            {<span id="messageCounter" class="badge">@ViewBag.Unread</span>}
            else
            {<span id="messageCounter" class="badge" style="display:none">@ViewBag.Unread</span> }</a></li>
        <li><a href="@Url.Action("Index","Friend")">Friends @if (ViewBag.NewFriends != 0)
            {<span id="friendsCounter" class="badge">@ViewBag.NewFriends</span>}
            else
            { <span id="friendsCounter" class="badge" style="display:none">@ViewBag.NewFriends</span>}</a></li>
        <li><a href="@Url.Action("Settings","Home")">Settings</a></li>
    </ul>
    <footer>
        <ul class="list-inline">
            <li class="col-md-4"><a >Help</a></li>
            <li class="col-md-4"><a>About us</a></li>

        </ul>
        <br/>
        <hr/>
        <p class="footer"><a>© 2017 – приложение ASP.NET</a></p>
    </footer>
</div>
